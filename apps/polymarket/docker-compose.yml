services:
  backend:
    build:
      context: ./backend
    environment:
      DATABASE_URL: sqlite:////app/data/ultramar.db
      LOG_LEVEL: INFO
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
