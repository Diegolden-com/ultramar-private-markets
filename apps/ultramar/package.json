{
  "name": "@ultramar/ultramar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "yarn run -T next dev",
    "build": "yarn run -T next build",
    "start": "yarn run -T next start",
    "lint": "yarn run -T eslint .",
    "typecheck": "yarn run -T tsc --noEmit",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "@supabase/ssr": "0.10.3",
    "@supabase/supabase-js": "2.105.4",
    "@ultramar/product-model": "workspace:*",
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "three": "0.184.0"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "@types/three": "0.184.1",
    "axe-core": "4.11.1"
  }
}
